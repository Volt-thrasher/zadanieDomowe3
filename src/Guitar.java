class Guitar {
    String producer;
    String colour;
    int strings;
    boolean electric;

    Guitar(String prod, String clr, int s, boolean el ) {
        producer = prod;
        colour = clr;
        strings = s;
        electric = el;
    }
}
